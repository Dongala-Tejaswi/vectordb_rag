# Vector DB RAG Project

## Steps to Run

1. Install dependencies:
   pip install -r requirements.txt

2. Add files in data/ folder

3. Run:
   python ingest.py

4. Start app:
   streamlit run app.py
